// Getting the length of the property, which is the same as
// the total amount on the page.
$(document).ready(function() {

  // same as next
  $('#get-length').click(function() {
    alert('Length = ' + $('p').length);
  });
  
});
